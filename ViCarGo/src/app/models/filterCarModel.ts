export interface FilterCarModel{
  selectedBrand: string;
  selectedTransmission: string;
  minPrice: number;
  maxPrice: number;
  selectedRadius: number;
  newerThan: string;
  startDate: any;
  endDate: any;
}
